import { View, Text, ScrollView, SafeAreaView, StyleSheet } from "react-native";
import { Redirect } from "expo-router";

import HomeIndex from "./(tabs)/home/index"

export default App = () => {
    return <Redirect href={'/(tabs)/home'}/>
}
